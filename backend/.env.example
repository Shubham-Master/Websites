NODE_ENV=development
PORT=4000
HOST=0.0.0.0
APP_URL=http://localhost:4000
# Use * only for local development. In production set your frontend domain(s), comma-separated.
CORS_ORIGIN=*
BOOKING_HOLD_MINUTES=10
TRUST_PROXY=false
